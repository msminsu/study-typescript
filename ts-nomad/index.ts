

const sayHi = (name:string , age: number, gender: string) : void => {
	console.log(`Hello ${name} , you are ${age} you are a  ${gender} test`);
};

console.log(sayHi("minsu", 333, "male"));
export {};